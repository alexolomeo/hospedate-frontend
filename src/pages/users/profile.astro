---
import ProfileDetail from '@/components/React/Profile/ProfileDetail';
import ProtectedLayout from '@/layouts/ProtectedLayout.astro';
import { detectLanguage } from '@/utils/i18n';

const lang = detectLanguage(Astro.request.headers, new URL(Astro.request.url));
---

<ProtectedLayout title={'Perfil'}>
  <ProfileDetail client:idle lang={lang} />
</ProtectedLayout>
